# Install
```
npm install
```
# Run
```
# Now run example start producer to topic "topic1"
node producer.js -f kafka.config -t topic1
# Now run example start consumer to topic "topic1"
node consumer.js -f kafka.config -t topic1
```

# Build
```
dotnet build
```
# Run
```
# Now run example start producer to topic "topic1"
dotnet run produce topic1 kafka.config
# Now run example start consumer to topic "topic1"
dotnet run consume testc1 kafka.config
```
